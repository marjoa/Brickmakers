/* */
ARG OPT 2
IF OPT="T" THEN CALL TOFLOPPY; ELSE CALL FROMFLOPPY
IF STREAM(SOURCE,"C","QUERY EXISTS")=="" THEN DO; SAY "Source does not exist"; EXIT; END
IF STREAM(TARGET,"C","QUERY EXISTS")\="" THEN DO; SAY "Target already exists"; CALL SYSDESTROYOBJECT TARGET; END
CALL SYSCOPYOBJECT SOURCE,TARGET
"@COMP """SOURCE""" "TARGET"\*"
IF RC=0 THEN SAY "File copied OK."
ELSE SAY "ERROR!"
EXIT

TOFLOPPY: /* Copy file TO drive A: */
SOURCE="C:\DESCNET\DATA\MUM\Brick Makers"
TARGET="A:\"
SAY "Copying TO the floppy"
RETURN

FROMFLOPPY: /* Copy file FROM drive A: */
SOURCE="A:\Brick Makers"
TARGET="C:\DESCNET\DATA\MUM"
SAY "Copying FROM the floppy"
RETURN
